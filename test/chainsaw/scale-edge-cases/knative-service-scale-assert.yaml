apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: test-function-scale-edge
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/max-scale: "0"
