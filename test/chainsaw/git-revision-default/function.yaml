apiVersion: functions.zenith.com/v1alpha1
kind: Function
metadata:
  name: test-function-revision-default
spec:
  # NOTE: gitRevision is intentionally omitted to test default behavior
  gitRepo: https://github.com/LucasGois1/zenith-test-functions
  gitAuthSecretName: github-auth
  build:
    image: registry.registry.svc.cluster.local:5000/zenith-test/revision-default:latest
  deploy:
    dapr:
      enabled: false
      appID: ""
      appPort: 8080
