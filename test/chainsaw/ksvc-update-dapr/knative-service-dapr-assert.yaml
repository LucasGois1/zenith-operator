apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: test-function-dapr-update
spec:
  template:
    metadata:
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: test-function-dapr-update
        dapr.io/app-port: "3000"
