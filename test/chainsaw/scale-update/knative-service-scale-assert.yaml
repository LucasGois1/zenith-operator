apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: test-function-scale-update
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "2"
        autoscaling.knative.dev/max-scale: "20"
