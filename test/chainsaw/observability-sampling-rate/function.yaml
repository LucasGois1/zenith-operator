apiVersion: functions.zenith.com/v1alpha1
kind: Function
metadata:
  name: test-function-sampling
spec:
  gitRepo: https://github.com/LucasGois1/zenith-test-functions
  gitRevision: devin/1763232295-add-test-functions
  gitAuthSecretName: github-auth
  build:
    image: registry.registry.svc.cluster.local:5000/zenith-test/sampling-rate:latest
  deploy:
    dapr:
      enabled: false
      appID: ""
      appPort: 8080
  observability:
    tracing:
      enabled: true
      samplingRate: "0.5"
