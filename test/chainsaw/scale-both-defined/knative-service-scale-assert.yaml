apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: test-function-scale-both
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "1"
        autoscaling.knative.dev/max-scale: "10"
