apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: test-function-trigger-update-trigger
spec:
  broker: test-broker
  filter:
    attributes:
      type: com.example.event.v2
      source: /test/source
