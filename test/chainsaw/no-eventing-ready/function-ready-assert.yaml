apiVersion: functions.zenith.com/v1alpha1
kind: Function
metadata:
  name: test-function-no-eventing
status:
  conditions:
    - type: Ready
      status: "True"
      reason: Ready
      message: (contains($, 'ready'))
  imageDigest: (exists($))
