apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: test-function-http-basic
spec:
  template:
    spec:
      containers:
        - image: (contains(@, 'registry.registry.svc.cluster.local:5000/zenith-test/test-function-http-basic'))
