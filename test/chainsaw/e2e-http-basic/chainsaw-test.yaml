apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: e2e-http-basic-test
spec:
  description: |
    Test end-to-end HTTP request flow through Kong to Function.
    This test verifies:
    1. Function deploys successfully
    2. Kong proxy is accessible via NodePort
    3. HTTP requests with Host header reach the function
    4. Function returns expected JSON response
  timeouts:
    apply: 1m
    assert: 12m
    delete: 1m
  steps:
    - name: install-tekton-tasks
      description: Install Tekton tasks in the test namespace
      try:
        - apply:
            file: git-clone-task.yaml
        - apply:
            file: buildpacks-phases-task.yaml
    
    - name: create-git-auth-secret
      description: Create Git authentication secret
      try:
        - script:
            content: |
              cat <<'EOF' | sed "s#__GITHUB_TOKEN__#${GITHUB_TOKEN}#g" | kubectl apply -f - -n $NAMESPACE
              apiVersion: v1
              kind: Secret
              metadata:
                name: github-auth
                annotations:
                  tekton.dev/git-0: https://github.com
              type: kubernetes.io/basic-auth
              stringData:
                username: LucasGois1
                password: __GITHUB_TOKEN__
              EOF
    
    - name: create-function
      description: Create a Function CR
      try:
        - apply:
            file: function.yaml
    
    - name: wait-for-build
      description: Wait for PipelineRun to complete
      try:
        - assert:
            file: pipelinerun-success-assert.yaml
            timeout: 8m
    
    - name: verify-knative-service
      description: Verify Knative Service was created
      try:
        - assert:
            file: knative-service-assert.yaml
            timeout: 1m
    
    - name: verify-function-ready
      description: Verify Function reaches Ready status
      try:
        - assert:
            file: function-ready-assert.yaml
            timeout: 2m
    
    - name: get-function-url
      description: Get Function URL from status
      try:
        - script:
            content: |
              FUNCTION_URL=$(kubectl get function test-function-http-basic -n $NAMESPACE -o jsonpath='{.status.url}')
              echo "Function URL: $FUNCTION_URL"
              
              if [ -z "$FUNCTION_URL" ]; then
                echo "ERROR: Function URL not set in status"
                exit 1
              fi
              
              FUNCTION_HOST=$(echo "$FUNCTION_URL" | sed 's|http://||' | sed 's|https://||')
              echo "$FUNCTION_HOST" > /tmp/function-host.txt
              echo "Function Host: $FUNCTION_HOST"
    
    - name: get-kong-endpoint
      description: Get Kong proxy NodePort endpoint
      try:
        - script:
            content: |
              KONG_NODE_PORT=$(kubectl get svc kong-gateway-proxy -n kong -o jsonpath='{.spec.ports[?(@.name=="kong-proxy")].nodePort}')
              KONG_NODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type=="InternalIP")].address}')
              
              if [ -z "$KONG_NODE_PORT" ] || [ -z "$KONG_NODE_IP" ]; then
                echo "ERROR: Could not get Kong endpoint"
                exit 1
              fi
              
              echo "Kong endpoint: http://${KONG_NODE_IP}:${KONG_NODE_PORT}"
              echo "${KONG_NODE_IP}:${KONG_NODE_PORT}" > /tmp/kong-endpoint.txt
    
    - name: test-http-request
      description: Test HTTP request through Kong to Function
      try:
        - script:
            content: |
              FUNCTION_HOST=$(cat /tmp/function-host.txt)
              KONG_ENDPOINT=$(cat /tmp/kong-endpoint.txt)
              
              echo "Testing HTTP request..."
              echo "  Kong: http://${KONG_ENDPOINT}"
              echo "  Host: ${FUNCTION_HOST}"
              
              sleep 10
              
              RESPONSE=$(curl -s -H "Host: ${FUNCTION_HOST}" "http://${KONG_ENDPOINT}/" || echo "CURL_FAILED")
              
              if [ "$RESPONSE" = "CURL_FAILED" ]; then
                echo "ERROR: curl command failed"
                exit 1
              fi
              
              echo "Response: $RESPONSE"
              
              if echo "$RESPONSE" | grep -q '"status"' && echo "$RESPONSE" | grep -q '"type"'; then
                echo "SUCCESS: Received valid JSON response"
              else
                echo "ERROR: Response does not contain expected JSON fields"
                echo "Response was: $RESPONSE"
                exit 1
              fi
              
              if echo "$RESPONSE" | grep -q '"status":"ok"'; then
                echo "SUCCESS: Status is 'ok'"
              else
                echo "ERROR: Status is not 'ok'"
                exit 1
              fi
            timeout: 60s
    
    - name: cleanup
      description: Clean up resources
      try:
        - delete:
            ref:
              apiVersion: functions.zenith.com/v1alpha1
              kind: Function
              name: test-function-http-basic
