apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: ksvc-update-image-test
spec:
  description: |
    Test that Knative Service is updated when Function image changes.
    This test verifies:
    1. Function deploys successfully with initial image
    2. Updating the image digest triggers KService update
    3. Function status reflects the new image
    4. ObservedGeneration is updated correctly
  timeouts:
    apply: 1m
    assert: 12m
    delete: 1m
  steps:
    - name: install-tekton-tasks
      description: Install Tekton tasks in the test namespace
      try:
        - apply:
            file: git-clone-task.yaml
        - apply:
            file: buildpacks-phases-task.yaml
    
    - name: create-git-auth-secret
      description: Create Git authentication secret
      try:
        - script:
            content: |
              cat <<'EOF' | sed "s#__GITHUB_TOKEN__#${GITHUB_TOKEN}#g" | kubectl apply -f - -n $NAMESPACE
              apiVersion: v1
              kind: Secret
              metadata:
                name: github-auth
                annotations:
                  tekton.dev/git-0: https://github.com
              type: kubernetes.io/basic-auth
              stringData:
                username: LucasGois1
                password: __GITHUB_TOKEN__
              EOF
    
    - name: create-function
      description: Create a Function CR
      try:
        - apply:
            file: function.yaml
    
    - name: wait-for-initial-build
      description: Wait for initial PipelineRun to complete
      try:
        - script:
            content: |
              FN_NAME=test-function-update-image
              for i in $(seq 1 60); do
                PR_NAME=$(kubectl get pipelinerun -n "$NAMESPACE" -o jsonpath='{range .items[?(@.metadata.ownerReferences[0].name=="'"$FN_NAME"'")]}{.metadata.name}{"\n"}{end}' | tail -n1)
                [ -n "$PR_NAME" ] && break
                sleep 2
              done
              [ -z "$PR_NAME" ] && { echo "PipelineRun not found for $FN_NAME"; exit 1; }
              echo "Found PipelineRun: $PR_NAME"
              kubectl wait --for=condition=Succeeded=True "pipelinerun/$PR_NAME" -n "$NAMESPACE" --timeout=8m
            timeout: 600s
    
    - name: verify-initial-knative-service
      description: Verify that initial Knative Service was created
      try:
        - script:
            content: |
              IMAGE=$(kubectl get service.serving.knative.dev test-function-update-image -n "$NAMESPACE" -o jsonpath='{.spec.template.spec.containers[0].image}')
              test -n "$IMAGE" || { echo "ERROR: KService image is empty"; exit 1; }
              echo "SUCCESS: KService created with image: $IMAGE"
    
    - name: verify-initial-ready
      description: Verify Function reaches Ready status
      try:
        - assert:
            file: function-ready-assert.yaml
            timeout: 2m
    
    - name: capture-initial-image
      description: Capture initial image digest
      try:
        - script:
            content: |
              INITIAL_IMAGE=$(kubectl get function test-function-update-image -n $NAMESPACE -o jsonpath='{.status.imageDigest}')
              echo "Initial imageDigest: $INITIAL_IMAGE"
              echo "$INITIAL_IMAGE" > /tmp/initial-image.txt
    
    - name: simulate-image-update
      description: Simulate new build by updating imageDigest in status
      try:
        - script:
            content: |
              NEW_DIGEST="registry.registry.svc.cluster.local:5000/zenith-test/test-function-update-image@sha256:$(openssl rand -hex 32)"
              kubectl patch function test-function-update-image -n $NAMESPACE --type=merge --subresource=status -p "{\"status\":{\"imageDigest\":\"$NEW_DIGEST\"}}"
              echo "Updated imageDigest to: $NEW_DIGEST"
              echo "$NEW_DIGEST" > /tmp/new-digest.txt
    
    - name: trigger-reconciliation
      description: Trigger reconciliation to pick up new image
      try:
        - script:
            content: |
              kubectl annotate function test-function-update-image -n $NAMESPACE reconcile-trigger="$(date +%s)" --overwrite
    
    - name: verify-ksvc-image-updated
      description: Verify Knative Service image was updated
      try:
        - script:
            content: |
              NEW_DIGEST=$(cat /tmp/new-digest.txt)
              sleep 5  # Give controller time to reconcile
              
              KSVC_IMAGE=$(kubectl get service.serving.knative.dev test-function-update-image -n $NAMESPACE -o jsonpath='{.spec.template.spec.containers[0].image}')
              
              if [ "$KSVC_IMAGE" = "$NEW_DIGEST" ]; then
                echo "SUCCESS: KService image updated to $NEW_DIGEST"
              else
                echo "ERROR: KService image is $KSVC_IMAGE, expected $NEW_DIGEST"
                exit 1
              fi
            timeout: 30s
    
    - name: verify-image-changed
      description: Verify image digest was actually changed
      try:
        - script:
            content: |
              INITIAL_IMAGE=$(cat /tmp/initial-image.txt)
              CURRENT_IMAGE=$(kubectl get function test-function-update-image -n $NAMESPACE -o jsonpath='{.status.imageDigest}')
              
              if [ "$CURRENT_IMAGE" != "$INITIAL_IMAGE" ]; then
                echo "SUCCESS: Image digest changed from $INITIAL_IMAGE to $CURRENT_IMAGE"
              else
                echo "ERROR: Image digest not changed (still: $CURRENT_IMAGE)"
                exit 1
              fi
    
    - name: cleanup
      description: Clean up resources
      try:
        - delete:
            ref:
              apiVersion: functions.zenith.com/v1alpha1
              kind: Function
              name: test-function-update-image
